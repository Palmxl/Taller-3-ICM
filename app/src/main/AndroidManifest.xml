<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos generales -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Ubicación -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Cámara y almacenamiento -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Desde Android 13 (Tiramisu), este permiso cambió -->
    <uses-permission
        android:name="android.permission.READ_MEDIA_IMAGES"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Hardware opcional (no obligatorio) -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Taller3ICM"
        tools:targetApi="34">

        <!-- Pantalla inicial -->
        <activity
            android:name=".auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Mapa de seguimiento (OSMDroid) -->
        <activity
            android:name="com.example.taller_3_icm.FollowMapActivity"
            android:exported="false" />

        <!-- Mapa principal -->
        <activity
            android:name=".HomeMapActivity"
            android:exported="false" />

        <!-- Registro -->
        <activity
            android:name=".auth.RegisterActivity"
            android:exported="false" />

        <!-- Bienvenida / perfil -->
        <activity
            android:name=".auth.WelcomeActivity"
            android:exported="false" />

        <!-- Menú principal -->
        <activity
            android:name=".menu.MenuActivity"
            android:exported="false" />

        <!-- Lista de usuarios -->
        <activity
            android:name=".menu.ListaDisponiblesActivity"
            android:exported="false" />

        <!-- Ver ubicación de otros -->
        <activity
            android:name=".menu.VerPosicionActivity"
            android:exported="false" />

    </application>
</manifest>

